let numbers = [1,2,3];

const num = new Array(3,4,5,6,7,8,9);//array can also be created like this and this method "Array(no in array)" contains all the funtion which can be used or aplly in array ex : join(),add(),filter()etc
//we can also use array like this    let numbers = [1,2,3];  //itt also contains all functions

// console.log(Object.getPrototypeOf(numbers));     //this is the proto of our array named above as number
console.log(Array.prototype);
console.log(numbers);

function hello(){
    console.log("hello");
}
